[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "aalf"
version = "0.1.0"
description = "Autopoietic Artificial Life Forms — Subscription-Driven Simulation"
requires-python = ">=3.10"

[project.scripts]
aalf = "aalf.__main__:main"
